{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "campusmate/coze/schemas/match_teammates.input.json",
  "title": "MatchTeammatesInput",
  "description": "工作流③智能匹配的外部输入。与后端 ai_match_teammates 完全一致：调用方只传 post_id。候选人资料由工作流通过受控后端接口获取；接口未就绪时不得配置 COZE_WORKFLOW_MATCH，应使用后端数据库 + LLM fallback。",
  "type": "object",
  "required": ["post_id"],
  "additionalProperties": false,
  "properties": {
    "post_id": {
      "type": "string",
      "minLength": 1,
      "description": "待匹配组队帖 ID。工作流必须经受控接口换取已脱敏的帖子、现有成员和候选用户上下文。"
    }
  }
}
